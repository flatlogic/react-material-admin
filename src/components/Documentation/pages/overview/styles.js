import { makeStyles } from "styles/mui";

export default makeStyles(theme => ({
    link: {
        display: 'flex',
        alignItems: 'center',
        color: theme.palette.primary.main,
        fontWeight: 'bold'
    }
}))
